package com.grozzbear.ui.theme

import androidx.compose.ui.graphics.Color

/** Main screen fill (login/register hero backgrounds often match this family). */
val GrozzBackground = Color(0xFF181F26)

/** Slightly deeper bar / scaffold color used by bottom nav and system bars. */
val GrozzSystemBar = Color(0xFF121417)

/** Fields, sheets, elevated panels. */
val GrozzSurface = Color(0xFF1C2126)

/** Borders / dividers on dark surfaces. */
val GrozzBorder = Color(0xFF2E353D)

/** Brand accent — buttons, highlights, selected states. */
val GrozzYellow = Color(0xFFF1C40F)

/** Placeholder / secondary labels. */
val GrozzMuted = Color(0xFF4B5F71)

/** Soft secondary text (e.g. “Forget password”). */
val GrozzTextSecondary = Color(0xFFD9D9D9)

val GrozzOnPrimary = Color.Black
val GrozzOnBackground = Color.White
val GrozzError = Color(0xFFFF4444)
