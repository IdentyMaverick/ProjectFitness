package com.grozzbear.ui.theme

import androidx.compose.ui.unit.dp

/** Photo cards (workout pickers, catalogue, your plans). */
val GrozzRadiusPhoto = 18.dp

/** Surface panels (exercise rows, settings, FAQ, follow list). */
val GrozzRadiusPanel = 16.dp

/** Chips, small buttons, inner fields. */
val GrozzRadiusChip = 12.dp

/** Primary CTA. */
val GrozzRadiusButton = 20.dp
